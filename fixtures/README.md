Golden vectors under `golden-vectors/` are generated by `helix-server`'s
`scripts/generate-golden-vectors.ts` (see that repo) and consumed here by
`helix-sdk-js/tests/unit/golden-vectors.test.ts` to verify cross-implementation
consistency (VC canonicalization/signing) between the server and this SDK.

This is a duplicated copy, not the source of truth — when helix-server
regenerates these vectors, re-copy them here too.
